# 🐧 Linux & CLI – Complete Study Material (Terraform-Focused)

This is **beginner-to-intermediate Linux & CLI study material**, focused **exactly on what you need for Terraform + Azure work**.

✔ Hands-on
✔ Practical
✔ Job-ready
✔ No unnecessary theory

---

## 1️⃣ Linux File System Basics (Foundation)

### 🔹 Important Directories

| Directory | Purpose               |
| --------- | --------------------- |
| `/`       | Root directory        |
| `/home`   | User home directories |
| `/etc`    | Configuration files   |
| `/var`    | Logs, variable data   |
| `/tmp`    | Temporary files       |
| `/usr`    | Installed programs    |

### 🔹 Check where you are

```bash
pwd
```

---

## 2️⃣ Navigation Commands (`cd`, `ls`)

### 📂 `cd` – Change Directory

```bash
cd /home/username
cd ..
cd ~
cd -
```

### 📂 `ls` – List Files

```bash
ls
ls -l
ls -a
ls -lh
```

### 🔹 Common combo

```bash
ls -la
```

---

## 3️⃣ Directory & File Creation (`mkdir`, `touch`)

### 📁 Create directories

```bash
mkdir terraform
mkdir -p project/dev/network
```

### 📄 Create files

```bash
touch main.tf variables.tf outputs.tf
```

---

## 4️⃣ Removing Files & Directories (`rm`)

⚠️ **Be careful – no undo!**

```bash
rm file.txt
rm -r terraform
rm -rf terraform   # dangerous
```

---

## 5️⃣ Viewing File Content

### 📄 Read files

```bash
cat file.txt
less file.txt
more file.txt
```

### 📄 View logs (real use case)

```bash
tail -f /var/log/syslog
```

---

## 6️⃣ Editing Files – `vi` / `nano` (VERY IMPORTANT)

### ✍️ Nano (Beginner Friendly)

```bash
nano main.tf
```

| Action   | Shortcut   |
| -------- | ---------- |
| Save     | `Ctrl + O` |
| Exit     | `Ctrl + X` |
| Cut line | `Ctrl + K` |
| Paste    | `Ctrl + U` |

👉 **Recommended when starting Terraform**

---

### ✍️ Vi / Vim (Industry Standard)

```bash
vi main.tf
```

#### Modes

* **Normal** – navigation
* **Insert** – editing (`i`)
* **Command** – save/exit (`:`)

#### Common Commands

| Action            | Command |
| ----------------- | ------- |
| Insert mode       | `i`     |
| Save              | `:w`    |
| Save & exit       | `:wq`   |
| Exit without save | `:q!`   |
| Delete line       | `dd`    |
| Undo              | `u`     |

---

## 7️⃣ Running CLI Commands (Critical for Terraform)

### 🔹 Basic command structure

```bash
command [options] [arguments]
```

Example:

```bash
ls -lh /var/log
```

### 🔹 Important CLI Commands

```bash
clear
history
whoami
hostname
date
```

---

## 8️⃣ Permissions & Ownership (Must Know)

### 🔐 Check permissions

```bash
ls -l
```

### 🔐 Change permissions

```bash
chmod 755 script.sh
chmod +x terraform.sh
```

### 🔐 Change ownership

```bash
chown user:user file.txt
```

---

## 9️⃣ Environment Variables (Terraform Uses This)

```bash
export ARM_CLIENT_ID="xxxx"
export ARM_CLIENT_SECRET="xxxx"
export ARM_TENANT_ID="xxxx"
export ARM_SUBSCRIPTION_ID="xxxx"
```

Check:

```bash
env
echo $ARM_CLIENT_ID
```

---

## 🔟 Package Management (Ubuntu)

```bash
sudo apt update
sudo apt install terraform
```

---

## 1️⃣1️⃣ Process Management (Basics)

```bash
ps -ef
top
kill -9 PID
```

---

## 1️⃣2️⃣ Real Terraform-Style Practice

### 📁 Create project structure

```bash
mkdir terraform-azure
cd terraform-azure
touch main.tf variables.tf outputs.tf
```

### ✍️ Edit Terraform file

```bash
nano main.tf
```

---

## 1️⃣3️⃣ Common Mistakes to Avoid ❌

* Using `rm -rf /`
* Editing files as root unnecessarily
* Forgetting to save in `vi`
* Not checking directory before running commands

---

## 📌 Daily Practice Plan (7 Days)

| Day | Practice              |
| --- | --------------------- |
| 1   | `cd`, `ls`, `pwd`     |
| 2   | `mkdir`, `rm`         |
| 3   | `nano`                |
| 4   | `vi`                  |
| 5   | Permissions           |
| 6   | Env variables         |
| 7   | Full practice project |

---

## 🎯 You Are READY When You Can


✅ Navigate directories without thinking

✅ Edit files confidently

✅ Understand permissions

✅ Run Terraform & Azure CLI commands

---
